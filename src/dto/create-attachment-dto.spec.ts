import { CreateAttachmentDto } from './create-attachment-dto';

describe('CreateAttachmentDto', () => {
  it('should be defined', () => {
    expect(new CreateAttachmentDto()).toBeDefined();
  });
});
