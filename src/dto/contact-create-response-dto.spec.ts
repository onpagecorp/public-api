import { ContactCreateResponseDto } from './contact-create-response-dto';

describe('ContactCreateResponseDto', () => {
  it('should be defined', () => {
    expect(new ContactCreateResponseDto()).toBeDefined();
  });
});
