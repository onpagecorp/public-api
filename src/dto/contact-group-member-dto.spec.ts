import { ContactGroupMemberDto } from './contact-group-member-dto';

describe('ContactGroupMemberDto', () => {
  it('should be defined', () => {
    expect(new ContactGroupMemberDto()).toBeDefined();
  });
});
