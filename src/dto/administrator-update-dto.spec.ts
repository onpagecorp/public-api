import { AdministratorUpdateDto } from './administrator-update-dto';

describe('AdministratorDto', () => {
  it('should be defined', () => {
    expect(new AdministratorUpdateDto()).toBeDefined();
  });
});
